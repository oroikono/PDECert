{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/oroikono/PDECert/schema/problem-template-v1.schema.json",
  "title": "PDECert candidate-free problem template",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "template_version",
    "name",
    "solution_semantics",
    "variables",
    "domains",
    "parameters",
    "field_names",
    "pde_residuals",
    "conditions"
  ],
  "properties": {
    "template_version": { "const": 1 },
    "name": { "type": "string", "minLength": 1 },
    "solution_semantics": { "const": "classical_strong" },
    "variables": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "type": "string", "pattern": "^[A-Za-z_][A-Za-z0-9_]*$" }
    },
    "domains": {
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "prefixItems": [{ "type": "number" }, { "type": "number" }],
        "minItems": 2,
        "maxItems": 2
      }
    },
    "parameters": {
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "uniqueItems": true,
        "items": {
          "enum": [
            "integer",
            "negative",
            "nonnegative",
            "nonpositive",
            "nonzero",
            "positive"
          ]
        }
      }
    },
    "field_names": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "type": "string", "pattern": "^[A-Za-z_][A-Za-z0-9_]*$" }
    },
    "pde_residuals": { "$ref": "#/$defs/constraints" },
    "conditions": { "$ref": "#/$defs/constraints" }
  },
  "$defs": {
    "constraints": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "expression"],
        "properties": {
          "name": { "type": "string", "minLength": 1 },
          "expression": { "type": "string", "minLength": 1 }
        }
      }
    }
  }
}
